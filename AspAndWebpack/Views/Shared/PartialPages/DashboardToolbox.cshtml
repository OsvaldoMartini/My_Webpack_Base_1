@using IHS.Apps.CMP.Models
@model IEnumerable<DashboardWidget>


@{
   // var toolboxWidgets = Model.DashboardWidgets.Where(p => p.Column == 0 && p.Widget != null).OrderBy(p => p.Widget.Name);

    foreach (DashboardWidget toolboxWidget in Model)
    {
        @Html.Partial("Widget/WidgetBase", toolboxWidget)
    }
}
        

