/**
 * A view model to represent the range quick pick option.
 * @param {string} text - The text to use as the display option.
 * @param {string} from - The value to apply as the from option.
 * @param {string} to - The value to apply as the to option.
 */

var FacetRangeQuickPickVM = function (text, from, to) {
    var self = this;

    self.displayText = text;
    self.to = to;
    self.from = from;
};